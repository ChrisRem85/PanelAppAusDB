# Configuration for PanelApp Australia Data Extraction

# API Configuration
BASE_URL=https://panelapp-aus.org/api
API_VERSION=v1
SWAGGER_URL=https://panelapp-aus.org/api/docs/?format=openapi

# Expected API version (will warn if different)
EXPECTED_API_VERSION=v1

# Output Configuration
DEFAULT_OUTPUT_PATH=../data

# HTTP Configuration
REQUEST_TIMEOUT=30
MAX_RETRIES=3

# Safety Configuration
MAX_PAGES=10000  # Prevent infinite loops